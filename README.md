# QUOTES API

# Endpoints:

Get all records:
GET to /quotes

Get single record:
GET to /quotes/{id}

Post record:
Must post "author", "quote", "book" and "year" columns
POST to /quotes/create

Update record:
PUT to /quotes/{id}

Delete a record:
DELETE to /quotes/{id}